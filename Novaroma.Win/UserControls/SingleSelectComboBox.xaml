<UserControl x:Class="Novaroma.Win.UserControls.SingleSelectComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             MinWidth="150">
    <Grid DataContext="{Binding}">
        <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding TmpSelectedItem}" 
                 BorderBrush="{StaticResource AccentColorBrush}" BorderThickness="1 1 1 1"
                 ItemTemplate="{StaticResource ItemSelectionDataTemplate}"/>
    </Grid>
</UserControl>
